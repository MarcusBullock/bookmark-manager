<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL:   <%= link.url   %>
      <% alltags = [] %>
      <% link.tags.each { |x| alltags << x.name } %>
      Tag: <%= alltags.join(' ') %>
    </li>
  <% end %>
</ul>
